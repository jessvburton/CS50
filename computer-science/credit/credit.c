#include <stdio.h>
#include <cs50.h>
int main(void)

{
    long card;
    //get card number
    {
        card = get_long("card number: ");
    }

    //multiply every other number by 2 then add together

    //add the answer to the digits previously excluded
    answer = 

    //PRINT ANSWER
    if (answer = )
    {
        printf("AMEX");
    }

    else if (answer = )
    {
        printf("MASTERCARD");
    }

    else if (answer = )
    {
        printf("VISA");
    }

    else (answer = )
    {
        printf("invalid");
    }
}
